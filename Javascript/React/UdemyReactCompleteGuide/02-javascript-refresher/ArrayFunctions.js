// Array functions use the arrow function syntax to perform operations on the elements of an array
const numbers = [1, 2, 3];
const doubleNumArray = numbers.map((x) => x * 2);
console.log(doubleNumArray);
